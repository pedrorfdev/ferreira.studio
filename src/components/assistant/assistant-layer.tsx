// components/assistant/assistant-layer.tsx
// Stub — implementado na Phase 4
import type { ProjectData } from "@/types/project"
interface Props { project: ProjectData }
export function AssistantLayer({ project: _ }: Props) {
    return null
}